projection implementation in class ZCL_C_SUB_PROJ unique;
strict ( 2 );
use draft;
use side effects;
define behavior for ZC_SUB_PROJ alias SubProject
use etag

{
  use create;
  use update;
  use event ApiCalled;

  field ( readonly ) Status, Message;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}